# URL base del API de SlopGuard (sin trailing slash).
# En desarrollo local el API corre en el puerto 8000 por defecto.
# En producción apunta al dominio del backend desplegado.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
