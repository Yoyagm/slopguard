# @slopguard/web

Frontend del SaaS de SlopGuard (Hito 5). **Placeholder.**

El scaffold real de Next.js (App Router, sistema de diseño, vistas) se construye en la
**Ola 6** (tareas H5-T31..T38) con `developer-complex` + skills de UI/UX senior
(`senior-frontend`, `ui-design-system`, `ux-researcher-designer`, `a11y-audit`).

Consume la API de `apps/api` (`/api/v1/...`). Ver `specs/slopguard-hito5-saas/design.md` §4 y §8.
